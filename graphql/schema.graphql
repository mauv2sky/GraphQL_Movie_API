type Person{
    id: Int!,
    name: String!,
    gender: String!,
    age: Int!,
}

type Query{
    people: [Person]!
    person(id: Int!) : Person
}

type Mutation{
    addPerson(name: String!, gender: String!, age: Int!): Person!,
    leavePerson(id: Int!) : Boolean
}